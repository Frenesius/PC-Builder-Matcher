// MATCH A MOBO WITH THE CPU BASED ON SOCKET
	MATCH (n)-->(c)-->(h), (x)-->(y)-->(h)
	WHERE n.EAN = '0884962471586, 4948382686046' AND y.Label = 'MOTHERBOARD' AND x.Socket = n.Socket
	RETURN n, x
	LIMIT 20
		
//	MATCH A MOBO WITH THE GPU THAT IS SELECTED

	




